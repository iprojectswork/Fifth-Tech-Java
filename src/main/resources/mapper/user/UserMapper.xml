<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fifthtech.dao.mapper.user.UserMapper">

    <!--
      用户分页：列与 sys_user / User 实体对齐（无 create_id/name、update_id/name、delete_id/name）。
      orgId 过滤：EXISTS sys_user_org。
    -->
    <select id="listPage" resultType="com.fifthtech.dao.entity.user.User">
        SELECT
            u.id,
            u.username,
            u.password,
            u.nickname,
            u.email,
            u.phone,
            u.status,
            u.create_time  AS createTime,
            u.update_time  AS updateTime,
            u.deleted,
            u.delete_time  AS deleteTime
        FROM sys_user u
        <where>
            u.deleted = 0
            <if test="query.username != null and query.username != ''">
                AND u.username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.nickname != null and query.nickname != ''">
                AND u.nickname LIKE CONCAT('%', #{query.nickname}, '%')
            </if>
            <if test="query.email != null and query.email != ''">
                AND u.email LIKE CONCAT('%', #{query.email}, '%')
            </if>
            <if test="query.phone != null and query.phone != ''">
                AND u.phone LIKE CONCAT('%', #{query.phone}, '%')
            </if>
            <if test="query.status != null">
                AND u.status = #{query.status}
            </if>
            <if test="query.orgIds != null and query.orgIds.size() &gt; 0">
                AND EXISTS (
                    SELECT 1
                    FROM sys_user_org uo
                    WHERE uo.user_id = u.id
                      AND uo.org_id IN
                      <foreach collection="query.orgIds" item="oid" open="(" separator="," close=")">
                          #{oid}
                      </foreach>
                )
            </if>
        </where>
        ORDER BY u.create_time DESC, u.id DESC
    </select>

</mapper>
