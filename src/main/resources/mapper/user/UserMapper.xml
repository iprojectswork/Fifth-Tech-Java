<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fifthtech.dao.mapper.user.UserMapper">

    <!--
      用户分页：列与 sys_user / User 实体对齐（无 create_id/name、update_id/name、delete_id/name）。
      orgId 过滤：EXISTS sys_user_org。
    -->
    <select id="listPage" resultType="com.fifthtech.dao.entity.user.User">
        SELECT
            sysUser.id,
            sysUser.username,
            sysUser.password,
            sysUser.nickname,
            sysUser.email,
            sysUser.phone,
            sysUser.status,
            sysUser.create_time  AS createTime,
            sysUser.update_time  AS updateTime,
            sysUser.deleted,
            sysUser.delete_time  AS deleteTime
        FROM sys_user sysUser
        <where>
            sysUser.deleted = 0
            <if test="query.username != null and query.username != ''">
                AND sysUser.username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.nickname != null and query.nickname != ''">
                AND sysUser.nickname LIKE CONCAT('%', #{query.nickname}, '%')
            </if>
            <if test="query.email != null and query.email != ''">
                AND sysUser.email LIKE CONCAT('%', #{query.email}, '%')
            </if>
            <if test="query.phone != null and query.phone != ''">
                AND sysUser.phone LIKE CONCAT('%', #{query.phone}, '%')
            </if>
            <if test="query.status != null">
                AND sysUser.status = #{query.status}
            </if>
            <if test="query.orgIds != null and query.orgIds.size() &gt; 0">
                AND EXISTS (
                    SELECT 1
                    FROM sys_user_org sysUserOrg
                    WHERE sysUserOrg.user_id = sysUser.id
                      AND sysUserOrg.org_id IN
                      <foreach collection="query.orgIds" item="orgId" open="(" separator="," close=")">
                          #{orgId}
                      </foreach>
                )
            </if>
        </where>
        ORDER BY sysUser.create_time DESC, sysUser.id DESC
    </select>

</mapper>
