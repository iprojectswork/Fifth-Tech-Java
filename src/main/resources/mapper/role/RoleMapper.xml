<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fifthtech.dao.mapper.role.RoleMapper">

    <!-- 根据用户ID查询角色列表（启用，鉴权用） -->
    <select id="selectByUserId" resultType="com.fifthtech.dao.entity.role.Role">
        SELECT sysRole.id,
               sysRole.role_name,
               sysRole.role_code,
               sysRole.description,
               sysRole.status,
               sysRole.sort,
               sysRole.create_id,
               sysRole.create_name,
               sysRole.create_time,
               sysRole.update_id,
               sysRole.update_name,
               sysRole.update_time,
               sysRole.deleted,
               sysRole.delete_id,
               sysRole.delete_name,
               sysRole.delete_time
        FROM sys_role sysRole
        INNER JOIN sys_user_role sysUserRole ON sysRole.id = sysUserRole.role_id
        WHERE sysUserRole.user_id = #{userId}
          AND sysRole.deleted = 0
          AND sysRole.status = 1
        ORDER BY sysRole.sort ASC
    </select>

    <!-- 用户详情：关联角色子表（含禁用） -->
    <select id="listRelatedByUserId" resultType="com.fifthtech.dao.entity.role.Role">
        SELECT sysRole.id,
               sysRole.role_name,
               sysRole.role_code,
               sysRole.description,
               sysRole.status,
               sysRole.sort,
               sysRole.create_id,
               sysRole.create_name,
               sysRole.create_time,
               sysRole.update_id,
               sysRole.update_name,
               sysRole.update_time,
               sysRole.deleted,
               sysRole.delete_id,
               sysRole.delete_name,
               sysRole.delete_time
        FROM sys_role sysRole
        INNER JOIN sys_user_role sysUserRole ON sysRole.id = sysUserRole.role_id
        WHERE sysUserRole.user_id = #{userId}
          AND sysRole.deleted = 0
        ORDER BY sysRole.sort ASC, sysRole.id ASC
    </select>

    <!-- 角色分页列表 -->
    <select id="listPage" resultType="com.fifthtech.dao.entity.role.Role">
        SELECT sysRole.id,
               sysRole.role_name,
               sysRole.role_code,
               sysRole.description,
               sysRole.status,
               sysRole.sort,
               sysRole.create_id,
               sysRole.create_name,
               sysRole.create_time,
               sysRole.update_id,
               sysRole.update_name,
               sysRole.update_time,
               sysRole.deleted,
               sysRole.delete_id,
               sysRole.delete_name,
               sysRole.delete_time
        FROM sys_role sysRole
        <where>
            sysRole.deleted = 0
            <if test="query.roleName != null and query.roleName != ''">
                AND sysRole.role_name LIKE CONCAT('%', #{query.roleName}, '%')
            </if>
            <if test="query.roleCode != null and query.roleCode != ''">
                AND sysRole.role_code LIKE CONCAT('%', #{query.roleCode}, '%')
            </if>
        </where>
        ORDER BY sysRole.sort ASC, sysRole.id ASC
    </select>

    <!-- /role/all：全部启用角色 -->
    <select id="selectAllEnabled" resultType="com.fifthtech.dao.entity.role.Role">
        SELECT sysRole.id,
               sysRole.role_name,
               sysRole.role_code,
               sysRole.description,
               sysRole.status,
               sysRole.sort,
               sysRole.create_id,
               sysRole.create_name,
               sysRole.create_time,
               sysRole.update_id,
               sysRole.update_name,
               sysRole.update_time,
               sysRole.deleted,
               sysRole.delete_id,
               sysRole.delete_name,
               sysRole.delete_time
        FROM sys_role sysRole
        WHERE sysRole.deleted = 0
          AND sysRole.status = 1
        ORDER BY sysRole.sort ASC, sysRole.id ASC
    </select>

    <!-- 根据角色ID查询权限ID列表 -->
    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        SELECT sysRolePermission.permission_id
        FROM sys_role_permission sysRolePermission
        INNER JOIN sys_permission sysPermission ON sysRolePermission.permission_id = sysPermission.id
        WHERE sysRolePermission.role_id = #{roleId}
          AND sysPermission.deleted = 0
    </select>

    <!-- 未删 roleCode 全局唯一性校验（excludeId 可选） -->
    <select id="countByRoleCode" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM sys_role sysRole
        WHERE sysRole.deleted = 0
          AND sysRole.role_code = #{roleCode}
          <if test="excludeId != null">
              AND sysRole.id &lt;&gt; #{excludeId}
          </if>
    </select>

</mapper>
